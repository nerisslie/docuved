<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.siren.docuved" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activity.DashboardAdmin"
            android:label="@string/title_activity_dashboard_admin" >
        </activity>
        <activity
            android:name=".ui.activity.DashboardSiswa"
            android:label="@string/title_activity_dashboard_siswa"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.activity.SplashScreen"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.DaftarSiswa" />
        <activity android:name=".ui.activity.Dashboard" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.activity.Profil" />
    </application>

</manifest>